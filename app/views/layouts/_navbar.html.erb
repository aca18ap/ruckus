<nav class="navbar navbar-expand-lg navbar-light bg-light d-flex justify-content-between">
        <a class="navbar-brand" href='/'><img src="logo.png" width="150" height="150" alt="">
        Ruckus
        </a>
        <div>
          <%= link_to 'Products', products_path, :class => "btn btn-primary" %>
          <%= link_to 'Users', users_path, :class => "btn btn-primary" %>
        </div>

        <div id="buttons">
            <% if user_signed_in? %>
              <%= link_to 'Edit account', edit_user_registration_path, :class => "btn btn-primary" %></li>
              <%= link_to 'Sign out', destroy_user_session_path, :method=>'delete', :class => "btn btn-primary" %></li>
            <% else %>
              <%= link_to 'Sign in', new_user_session_path, :class => "btn btn-primary" %></li>
              <%= link_to 'Sign up', new_user_registration_path, :class => "btn btn-primary" %></li>

            <% end %>    
        </div>
    </div>

</nav>